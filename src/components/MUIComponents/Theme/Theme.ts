import { createTheme } from '@mui/material/styles';

export const lightheme = createTheme({
    palette: {
        primary: {
        main: '#4caf50',
        light: '#80e27e', 
        dark: '#087f23',
        },
        secondary: {
        main: '#ff5722',
        },
    },
});