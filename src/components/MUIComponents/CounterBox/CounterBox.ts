import { Box, styled } from "@mui/material";

export const CounterBox = styled(Box)({
    display: 'flex',
    flexDirection: 'row' ,
    justifyContent: 'center',
    alignItems: 'center',
    flexWrap: 'wrap',
    gap: 30,
    p: 30,
    minWidth: '100vw',
    minHeight: '100vh',
    margin: '0 auto',
    background: '#fed470'
})

